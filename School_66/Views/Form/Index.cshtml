@{
    ViewData["Title"] = "Головна";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<section id="form-page" class="container" style="padding: 40px 20px;">
    <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:20px;">
        <h2 style="margin:0; text-align:left;">
            Ласкаво просимо до форми заповнення запиту/питань до навчальної частини
        </h2>

        <!-- Кнопка "Мої запити" -->
        <a asp-controller="Form" asp-action="GetForm" class="requests-btn">
            Мої запити
        </a>
    </div>

    <div style="margin-top:40px; margin-left:20px;">
        <p style="font-size:18px; margin-bottom:15px;">
            Виберіть, від кого робите запит:
        </p>
        <div style="display:flex; gap:15px; margin-bottom:30px;">
            <a id="studentBtn" href="/students/Get" class="modern-btn">
                <i class="bi bi-arrow-right" style="margin-right:8px;"></i>Учень
            </a>

            <a id="parentBtn" href="/parents/Get" class="modern-btn">
                <i class="bi bi-people" style="margin-right:8px;"></i>Батьки
            </a>
        </div>

    </div>
</section>

@section Scripts {
    <script>
        const studentBtn = document.getElementById("studentBtn");
        const parentBtn = document.getElementById("parentBtn");
        const studentForm = document.getElementById("studentForm");
        const parentForm = document.getElementById("parentForm");

        // Перемикач форм
        studentBtn.addEventListener("click", () => {
            studentForm.style.display = "block";
            parentForm.style.display = "none";
            studentBtn.classList.add("active");
            parentBtn.classList.remove("active");
        });

        parentBtn.addEventListener("click", () => {
            parentForm.style.display = "block";
            studentForm.style.display = "none";
            parentBtn.classList.add("active");
            studentBtn.classList.remove("active");
        });
    </script>

    <style>
        /* Сучасний стиль кнопок */
        .modern-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 6px; /* расстояние между иконкой и текстом */
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: #fff !important; /* чтобы у ссылки не было синего */
            padding: 12px 24px;
            border: none;
            border-radius: 30px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 500;
            text-decoration: none; /* убрать подчёркивание */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }

        .modern-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.25);
            text-decoration: none;
        }

        .modern-btn.active {
            background: linear-gradient(135deg, #28a745, #1e7e34);
        }


        /* Кнопка "Мої запити" */
        .requests-btn {
            background: #ffc107;
            color: #000;
            padding: 10px 20px;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
        }

        .requests-btn:hover {
            background: #e0a800;
            color: #fff;
        }
    </style>
}